---

import Layout from "../layouts/Layout.astro";
import Toolbar from "../components/Toolbar.astro";
import SectionMenuModelTwo from "../components/SectionsMenu/SectionMenuModelTwo.astro";
---
<Layout>
    <!-- <div class="tool-bar flex items-center justify-center">
        <img src={logo.src} alt="logo vaca tuerta">
    </div> -->
    <Toolbar sticky={true}   ></Toolbar>
    <div class="content-menu">
        <!-- <div class="section-title">
            <h2>Nuestro Menú</h2>
        </div> -->
       <div class="content-list">
        <SectionMenuModelTwo></SectionMenuModelTwo>
       </div>
    </div>
</Layout>
<style>
    .tool-bar{
        padding: 1rem 5rem;
       
    }
    img{
        max-width: 180px;
    }
    .content-list{
        /* padding-top: 10rem;
        width: 100%; */
        
    }

    .section-title {
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .section-title h2 {
            font-size: 2rem;
            color: var(--color-text);
            position: relative;
            display: inline-block;
            padding-bottom: 0.5rem;
        }
        
        
    .content-menu{

        padding: 0 1rem;
        padding-top: 2rem;
    }
    @media (max-width:500px) {
        .tool-bar img{
            max-width: 160px;
        }
        .section-title{
            margin-bottom: 1rem;
        }
     .section-title h2{
        font-size: 1.8rem;
     }   
    }
</style>
<script>
    function initFilters() {
      const filterButtons = document.querySelectorAll('.filter-button');
      const filterOptions = document.getElementById('filter-options');
  
      if (!filterOptions) return;
  
      filterButtons.forEach(button => {
        button.addEventListener('click', async () => {
        
  
          // Scroll al top solo en este componente
          window.scrollTo({ top: 0, behavior: 'smooth' });
        });
      });
    }
  
    document.addEventListener('DOMContentLoaded', initFilters);
    document.addEventListener('astro:page-load', initFilters);
  </script>