<!-- ---
import { dishes } from "../data/dishes"
import Layout from "../layouts/Layout.astro"
import type { Dish } from "../interface/dish"
import CardDishModelTwo from "../components/CardsDishes/CardDishModelTwo.astro";
interface dishRating {
    id: string,
    rating:number,
  }

let dishesWithRating: Dish[] = [];
const getDishesWithRating() {

// Obtener valoraciones guardadas
const savedRatings: dishRating[] = JSON.parse(localStorage.getItem('dishRatings'));
  console.log(savedRatings)
  if(savedRatings){
    savedRatings.forEach(dishSave=>{
        const dishIndex = dishes.findIndex(dish=> dish._id === dishSave.id);
        if(dishIndex != -1){
            dishesWithRating.push(dishes[dishIndex])
        }
    })
  }
}



---
<Layout>

    {dishesWithRating.map(dish=>(
        <CardDishModelTwo dish={dish} ></CardDishModelTwo>
    ))}
    
</Layout>

<script>
     
      
</script> -->