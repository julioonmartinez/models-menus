---
import SectionMenu from "../components/SectionsMenu/SectionMenu.astro";
import Toolbar from "./Toolbar.astro";
import hero from '/src/assets/hero-home.webp';
import { DEMOIMAGES } from "../data/demoImages";
import SectionMenuModelTwo from "./SectionsMenu/SectionMenuModelTwo.astro";
---
<Toolbar sticky={true} ></Toolbar>
<div class="hero bg-[url(/src/assets/hero-home.webp)] w-full bg-cover bg-center bg-no-repeat flex flex-col justify-center px-2 relative z-0" >
  <div class="relative z-10 text-white py-8 px-4 lg:pl-16  flex flex-col gap-4 max-w-3xl ">
    <!-- Tu contenido aquí -->
    <h1 class="text-4xl lg:text-5xl font-bold text-pretty">El arte del buen corte, servido en tu mesa"</h1>
    <p class="text-2xl lg:text-3xl text-pretty " >Cortes selectos, madurados a la perfección y asados con leña de roble</p>
    <div class="content-actions  gap-4 mt-1 lg:mt-1 " >
        
      <a href="/menu" class="btn text-white btn-menu  min-h-15 flex gap-5 rounded-[8px] justify-between items-center text-1xl font-bold bg-[var(--color-accent)] px-4 py-4 cursor-pointer" >
        <p class="text-white" >Menú</p>
        <i class="fa-solid fa-utensils text-white "></i>        
    </a>
      <a href="#reservas" class="btn btn-menu   min-h-15 flex justify-between gap-5 items-center rounded-[8px] text-1xl font-bold bg-[var(--color-primary)] text-[var(--color-text)] px-4 py-4 cursor-pointer" >
        <p  class="" >Reservaciones</p>
       <i class="fa-solid fa-arrow-right rotate-325"></i>
    </a>

    </div>
  </div>
</div>
<section id="menu">
    <div class="container">
        <div id="section-title" class="section-title">
            <h2>Nuestro Menú</h2>
        </div>
       <SectionMenuModelTwo  ></SectionMenuModelTwo>
    </div>
  </section>
<section class="about" id="nosotros">
  <div class="container">
      <div class="section-title">
          <h2>Nuestra Historia</h2>
      </div>
      <div class="about-content">
          <div class="about-text">
              <p>La Vaca Tuerta nació en 2010 como un pequeño restaurante familiar con una gran visión: ofrecer la mejor experiencia gastronómica con productos locales de la más alta calidad.</p>
              <p>Nuestra filosofía se basa en respetar el producto, potenciar sus sabores naturales y presentarlo de manera creativa pero sin artificios innecesarios.</p>
              <p>Hoy, La Vaca Tuerta es reconocido como uno de los mejores restaurantes de la región, manteniendo siempre nuestra esencia y compromiso con la excelencia.</p>
              <a href="#menu" class="btn">Ver Nuestro Menú</a>
          </div>
          <div class="about-img">
              <img src={hero.src} alt="Un gran bisteck">
          </div>
      </div>
  </div>
</section>

<section class="reservation" id="reservas">
  <div class="container">
      <div class="section-title">
          <h2>Reserva Tu Mesa</h2>
      </div>
      <div class="reservation-form grid place-content-center ">
          <!-- <form action="#" method="post">
              <div class="form-group">
                  <label for="name">Nombre Completo</label>
                  <input type="text" id="name" class="form-control" required>
              </div>
              <div class="form-group">
                  <label for="email">Correo Electrónico</label>
                  <input type="email" id="email" class="form-control" required>
              </div>
              <div class="form-group">
                  <label for="phone">Teléfono</label>
                  <input type="tel" id="phone" class="form-control" required>
              </div>
              <div class="form-group">
                  <label for="date">Fecha</label>
                  <input type="date" id="date" class="form-control" required>
              </div>
              <div class="form-group">
                  <label for="time">Hora</label>
                  <input type="time" id="time" class="form-control" required>
              </div>
              <div class="form-group">
                  <label for="guests">Número de Personas</label>
                  <input type="number" id="guests" min="1" max="12" class="form-control" required>
              </div>
              <div class="form-group">
                  <label for="message">Mensaje (Opcional)</label>
                  <textarea id="message" class="form-control" rows="4"></textarea>
              </div>
              <button type="submit" class="btn">Confirmar Reserva</button>
          </form> -->
          <iframe title="open table frame" width="224" height="400" src="//www.opentable.com/widget/reservation/canvas?rid=1379974&amp;domain=com&amp;type=standard&amp;theme=standard&amp;lang=es&amp;overlay=false&amp;insideiframe=true" ></iframe>
      </div>
  </div>
</section>

<section id="galeria">
  <div class="container">
      <div class="section-title">
          <h2>Galería</h2>
      </div>
      <div class="gallery">
          {DEMOIMAGES.map((image)=>(
            <div class="gallery-item">
              <img src={image} alt="Restaurante La Vaca Tuerta" loading="lazy"/>
          </div>
          ))}
          
      </div>
  </div>
</section>

<section class="contact" id="contacto">
  <div class="container">
      <div class="section-title">
          <h2>Contacto</h2>
      </div>
      <div class="contact-grid">
          <div class="contact-info">
              <div class="contact-card">
                  <i>📍</i>
                  <div>
                      <h4>Dirección</h4>
                      <p>Av. Principal 123, Ciudad</p>
                  </div>
              </div>
              <div class="contact-card">
                  <i>📞</i>
                  <div>
                      <h4>Teléfono</h4>
                      <p>+34 123 456 789</p>
                  </div>
              </div>
              <div class="contact-card">
                  <i>📧</i>
                  <div>
                      <h4>Correo</h4>
                      <p>info@lavacatuerta.com</p>
                  </div>
              </div>
              <div class="contact-card">
                  <i>🕒</i>
                  <div>
                      <h4>Horario</h4>
                      <p>Lunes - Domingo: 12:00 - 23:00</p>
                  </div>
              </div>
          </div>
          <div class="map">
              <div style="background-color: #ddd; height: 300px; border-radius: 10px; display: flex; align-items: center; justify-content: center;">
                  <p>Mapa del Restaurante</p>
              </div>
          </div>
      </div>
  </div>
</section>
<script>
    function initFilters() {
      const filterButtons = document.querySelectorAll('.filter-button');
      const filterOptions = document.getElementById('filter-options');
  
      if (!filterOptions) return;
  
      filterButtons.forEach(button => {
        button.addEventListener('click', async () => {
        
  
          // Scroll al top solo en este componente
          window.scrollTo({ top: 0, behavior: 'smooth' });
          var sectionTitle = document.getElementById('section-title');

          sectionTitle?.scrollIntoView({
            behavior:'smooth',
            block: 'start'
          })
        });
      });
    }
  
    document.addEventListener('DOMContentLoaded', initFilters);
    document.addEventListener('astro:page-load', initFilters);
</script>


<style>
  
  .hero{
   min-height: calc(100vh - 65px);
   
  }
  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.633); /* Ajusta la opacidad (0.5 = 50% oscuro) */
    z-index: 1;
  }
  .btn{
    /* background-color: var(--color); */
    transition: transform 0.3s ease-in-out;
    /* background: linear-gradient(135deg, #856648, #936b43); */
 
  /* transition: background 0.5s ease; */
}
  .btn:hover{
    transform: scale(1.01);
    /* background: linear-gradient(225deg, #856648, var(--color-accent)); */
  /* transform: scale(1.05); */
  }


  section {
            padding: 5rem 1rem;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 3rem;
        }
        
        .section-title h2 {
            font-size: 2.5rem;
            color: var(--color-text);
            position: relative;
            display: inline-block;
            padding-bottom: 0.5rem;
        }
        
        .section-title h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background-color: var(--color-accent);
        }
        
        .about {
            background-color: var(--color-primary);
        }
        
        .about-content {
            display: flex;
            align-items: center;
            gap: 2rem;
        }
        
        .about-text {
            flex: 1;
        }
        
        .about-img {
            flex: 1;
            border-radius: 10px;
            overflow: hidden;
        }
        
        .about-img img {
            width: 100%;
            height: auto;
            display: block;
        }
        

        .reservation {
            background-color: var(--color-secondary);
            text-align: center;
        }
        
        .reservation-form {
            max-width: 600px;
            margin: 0 auto;
            padding: 2rem;
            background-color: var(--color-light);
            border-radius: 10px;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
            text-align: left;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
        }
        
        .form-control {
            width: 100%;
            padding: 0.8rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 1rem;
        }
        
        .gallery {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1rem;
        }
        
        .gallery-item {
            border-radius: 8px;
            overflow: hidden;
            height: 250px;
        }
        
        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s;
        }
        
        .gallery-item:hover img {
            transform: scale(1.1);
        }
        
        .contact {
            background-color: var(--color-primary);
        }
        
        .contact-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }
        
        .contact-info {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }
        
        .contact-card {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            background-color: var(--color-light);
            border-radius: 8px;
        }
        
        .contact-card i {
            font-size: 1.5rem;
            color: var(--color-accent);
        }
        
        
        
        @media (max-width: 991px) {
            .about-content {
                flex-direction: column;
            }
            
            .hero h1 {
                font-size: 2.8rem;
            }
        }
        
        @media (max-width: 768px) {
            header {
                padding: 0.8rem;
            }
            
            .nav-container {
                position: relative;
            }
            
            nav {
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background-color: var(--color-primary);
                height: 0;
                overflow: hidden;
                transition: height 0.3s;
            }
            
            nav.active {
                height: auto;
            }
            
            nav ul {
                flex-direction: column;
                padding: 1rem;
            }
            
            nav ul li {
                margin: 0;
                padding: 0.8rem 0;
                border-bottom: 1px solid #ddd;
            }
            
            .mobile-menu {
                display: block;
            }
            
            .hero {
                height: 60vh;
            }
            
            .hero h1 {
                font-size: 2.2rem;
            }
            
            .section-title h2 {
                font-size: 2rem;
            }
        }
        
        @media (max-width: 480px) {
            .hero {
                height: 50vh;
            }
            
            .hero h1 {
                font-size: 1.8rem;
            }
            
            .btn {
                padding: 0.6rem 1.2rem;
            }
            
            section {
                padding: 3rem 1rem;
            }
            
            .section-title h2 {
                font-size: 1.8rem;
            }
        }
        .content-actions{
            display: flex;
            flex-direction: column;
            gap: 1rem;

        }
        @media (min-width:768px) {
            .content-actions{
                flex-direction: row;
            }
        }
</style>